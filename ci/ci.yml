# yaml-language-server: $schema=https://raw.githubusercontent.com/siemens/kas/master/kas/schema-kas.json

header:
  version: 14
  includes:
    - ci/meta-qcom.yml
    - repo: meta-qcom
      file: ci/ci.yml

local_conf_header:
  resource-limits: |
    BB_PRESSURE_MAX_CPU = "500000"
    BB_PRESSURE_MAX_IO = "500000"
    BB_PRESSURE_MAX_MEMORY = "100000"
