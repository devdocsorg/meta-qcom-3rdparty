# ci/yocto-patchreview.sh

## Index

* [_is_dir](#_is_dir)

### _is_dir

Validate that the supplied path is an existing directory.

#### Example

```bash
  _is_dir "$WORK_DIR"
@exitcode 0 The directory exists; no value is returned on stdout.
@exitcode 1 The path is not a directory; exits the calling script.
@stdout A diagnostic naming the invalid path on failure.
```

#### Arguments

* **$1** (string): Directory path, absolute or relative to the current working directory.

